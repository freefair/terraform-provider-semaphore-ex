---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_kubernetes_execution_policy Resource - Semaphore EX"
subcategory: ""
description: |-
  Manages the revision-fenced execution policy for one Semaphore EX Kubernetes cluster.
---

# semaphore_ex_kubernetes_execution_policy (Resource)

Manages the revision-fenced execution policy for one Semaphore EX Kubernetes cluster.

## Example Usage

```terraform
resource "semaphore_ex_kubernetes_execution_policy" "cluster" {
  cluster_alias              = "qa-cluster"
  allowed_namespaces         = ["semaphore-jobs"]
  allowed_images             = ["registry.example.test/job@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"]
  allowed_service_accounts   = ["semaphore-task"]
  allowed_runtime_classes    = [""]
  runtime_class              = ""
  allowed_volume_types       = ["emptyDir", "secret"]
  allowed_network_profiles   = ["deny-all"]
  network_profile            = "deny-all"
  network_policy_enforcement = "network-policy"
  resources = {
    cpu_request_milli               = 100
    cpu_limit_milli                 = 500
    memory_request_bytes            = 67108864
    memory_limit_bytes              = 268435456
    ephemeral_storage_request_bytes = 67108864
    ephemeral_storage_limit_bytes   = 268435456
  }
  terminal_retention_seconds = 3600
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `allowed_images` (Set of String)
- `allowed_namespaces` (Set of String)
- `allowed_network_profiles` (Set of String)
- `allowed_runtime_classes` (Set of String)
- `allowed_service_accounts` (Set of String)
- `allowed_volume_types` (Set of String)
- `cluster_alias` (String)
- `network_policy_enforcement` (String)
- `network_profile` (String)
- `resources` (Attributes) (see [below for nested schema](#nestedatt--resources))
- `runtime_class` (String)
- `terminal_retention_seconds` (Number)

### Read-Only

- `hash` (String)
- `id` (String) The ID of this resource.
- `revision` (Number)

<a id="nestedatt--resources"></a>
### Nested Schema for `resources`

Required:

- `cpu_limit_milli` (Number)
- `cpu_request_milli` (Number)
- `ephemeral_storage_limit_bytes` (Number)
- `ephemeral_storage_request_bytes` (Number)
- `memory_limit_bytes` (Number)
- `memory_request_bytes` (Number)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Replace illustrative identifiers with the existing object identity.
terraform import semaphore_ex_kubernetes_execution_policy.cluster cluster-alias
```
