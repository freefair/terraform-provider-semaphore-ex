---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_project_task_group Resource - Semaphore EX"
subcategory: ""
description: |-
  Manages a project task group in Semaphore EX v2.20.0-ex.2.1.1 or later. All groups selected by a template apply; admission reserves all capacity atomically. Updates and deletes use the last observed revision and never retry concurrent edits.
---

# semaphore_ex_project_task_group (Resource)

Manages a project task group in Semaphore EX v2.20.0-ex.2.1.1 or later. All groups selected by a template apply; admission reserves all capacity atomically. Updates and deletes use the last observed revision and never retry concurrent edits.

## Example Usage

```terraform
resource "semaphore_ex_project_task_group" "state" {
  project_id         = 1
  name               = "Production state"
  description        = "Serialize every template using the same Terraform state."
  max_parallel_tasks = 1
  runner_ids         = [4, 5]
  shared_project_ids = [2]
}

# Select the group on each participating template:
# task_groups = [semaphore_ex_project_task_group.state.id]
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name. Name must contain 1–128 bytes without surrounding whitespace, line breaks or NUL.
- `project_id` (Number) <i style="color:red;font-weight: bold">(ForceNew)</i> Owner project for a resource; project through which a data source reads the group (including an explicit project grant). Value must be at least 1.

### Optional

- `description` (String) Description. String length must be at most 2048.
- `max_parallel_tasks` (Number) Maximum concurrent executions across every template and project using this group. Defaults to 1 on create; omission preserves existing limits. Value must be between 1 and 1000.
- `runner_ids` (Set of Number) Allowed runner IDs. Omission preserves existing restrictions. Empty adds no runner restriction. Several groups intersect their runner restrictions. Element value must satisfy all validations: value must be at least 1.
- `shared_project_ids` (Set of Number) Projects allowed to select this group. Omission preserves existing grants; empty removes them. The owner project is implicit. A data source reading through a receiving project returns null because the API hides the full grant list. Element value must satisfy all validations: value must be at least 1.

### Read-Only

- `id` (Number) Task group ID.
- `owner_project_id` (Number) Project that owns the group and authorizes mutations.
- `revision` (Number) Server revision used to reject concurrent modifications.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import semaphore_ex_project_task_group.state project/1/task_group/7
```
