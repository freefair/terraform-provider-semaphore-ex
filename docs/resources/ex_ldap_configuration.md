---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_ldap_configuration Resource - Semaphore EX"
subcategory: ""
description: |-
  Configures an LDAP identity provider and its administrator-controlled exposure state.
---

# semaphore_ex_ldap_configuration (Resource)

Configures an LDAP identity provider and its administrator-controlled exposure state.

## Example Usage

```terraform
resource "semaphore_ex_ldap_configuration" "corporate" {
  id                       = "corporate"
  display_name             = "Corporate Directory"
  state                    = "disabled"
  server_url               = "ldaps://directory.example.test:636"
  tls_mode                 = "ldaps"
  trust_mode               = "system"
  bind_dn                  = "cn=semaphore,ou=service,dc=example,dc=test"
  bind_password            = var.ldap_bind_password
  bind_password_wo_version = 1
  search_base_dn           = "ou=people,dc=example,dc=test"
  user_filter              = "(uid={{username}})"
  identity_attribute       = "entryUUID"
  username_attribute       = "uid"
  name_attribute           = "cn"
  email_attribute          = "mail"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `bind_dn` (String)
- `display_name` (String)
- `email_attribute` (String)
- `identity_attribute` (String)
- `name_attribute` (String)
- `search_base_dn` (String)
- `server_url` (String)
- `state` (String)
- `tls_mode` (String)
- `trust_mode` (String)
- `user_filter` (String)
- `username_attribute` (String)

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `bind_password` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments))
- `bind_password_wo_version` (Number)
- `ca_pem` (String)
- `group_filter` (String)
- `group_identity_attribute` (String)
- `group_max_depth` (Number)
- `group_member_attribute` (String)
- `group_search_base_dn` (String)
- `group_user_filter` (String)
- `selected_user_ids` (List of Number)

### Read-Only

- `bind_password_configured` (Boolean)
- `id` (String) The ID of this resource.
