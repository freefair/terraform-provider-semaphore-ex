---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_project_terraform_backend_alias Resource - Semaphore EX"
subcategory: ""
description: |-
  Manage an alias and Basic-auth credential binding for one encrypted Terraform HTTP backend inventory.
---

# semaphore_ex_project_terraform_backend_alias (Resource)

Manage an alias and Basic-auth credential binding for one encrypted Terraform HTTP backend inventory.

## Example Usage

```terraform
resource "semaphore_ex_project_terraform_backend_alias" "state" {
  project_id   = 1
  inventory_id = 2
  auth_key_id  = 3
}

# Configure Terraform's HTTP backend with this alias and the selected
# login-password Access Key's login/password. Keep the password in an
# environment variable such as TF_HTTP_PASSWORD, never in this file.
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `auth_key_id` (Number) Project login-password key used as the HTTP backend Basic-auth credential.
- `inventory_id` (Number) <i style="color:red;font-weight: bold">(ForceNew)</i> Terraform, OpenTofu, or Terragrunt workspace inventory. Value must be at least 1.
- `project_id` (Number) <i style="color:red;font-weight: bold">(ForceNew)</i> Project containing the Terraform workspace. Value must be at least 1.

### Read-Only

- `id` (String) Opaque alias identifier assigned by Semaphore EX.
- `url` (String) HTTP backend address for this alias. Configure it as TF_HTTP_ADDRESS, TF_HTTP_LOCK_ADDRESS, and TF_HTTP_UNLOCK_ADDRESS.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import semaphore_ex_project_terraform_backend_alias.state project/1/inventory/2/alias/opaque-alias-id
```
