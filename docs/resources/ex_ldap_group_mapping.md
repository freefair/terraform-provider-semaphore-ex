---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_ldap_group_mapping Resource - Semaphore EX"
subcategory: ""
description: |-
  Maps an external identity-provider group to a custom role.
---

# semaphore_ex_ldap_group_mapping (Resource)

Maps an external identity-provider group to a custom role.

## Example Usage

```terraform
resource "semaphore_ex_ldap_group_mapping" "operators" {
  id                = "operators"
  provider_id       = "corporate"
  group_external_id = "entryuuid:12345678-1234-1234-1234-123456789abc"
  enabled           = true

  target = {
    scope   = "global"
    role_id = semaphore_ex_global_role.operator.id
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `enabled` (Boolean)
- `group_external_id` (String) Lowercase LDAP group external identifier to map.
- `provider_id` (String)
- `target` (Attributes) (see [below for nested schema](#nestedatt--target))

### Read-Only

- `claim_value` (String) Not applicable to LDAP mappings.
- `id` (String) The ID of this resource.
- `revision` (Number)

<a id="nestedatt--target"></a>
### Nested Schema for `target`

Required:

- `role_id` (String)
- `scope` (String)

Optional:

- `project_id` (Number)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Replace illustrative identifiers with the existing object identity.
terraform import semaphore_ex_ldap_group_mapping.operators provider-id/mapping/mapping-id
```
