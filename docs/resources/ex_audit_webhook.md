---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_audit_webhook Resource - Semaphore EX"
subcategory: ""
description: |-
  Manages the singleton Semaphore EX audit webhook configuration. Destroy pauses deliveries but retains the server configuration and credentials. It never sends test deliveries.
---

# semaphore_ex_audit_webhook (Resource)

Manages the singleton Semaphore EX audit webhook configuration. Destroy pauses deliveries but retains the server configuration and credentials. It never sends test deliveries.

## Example Usage

```terraform
resource "semaphore_ex_audit_webhook" "audit" {
  endpoint = "https://audit.example.com/events"
  paused   = true

  # Each changed value explicitly requests one new one-time signing secret.
  signing_bootstrap_version = 1
  signing_stage_version     = 1
}

output "current_audit_webhook_signing_secret" {
  value     = semaphore_ex_audit_webhook.audit.current_signing_secret
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `endpoint` (String)

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `credential_wo` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments))
- `credential_wo_version` (Number)
- `paused` (Boolean)
- `signing_bootstrap_version` (Number)
- `signing_stage_version` (Number)

### Read-Only

- `credential_configured` (Boolean)
- `current_generation` (Number)
- `current_key_id` (String)
- `current_signing_secret` (String, Sensitive)
- `id` (String) The ID of this resource.
- `next_generation` (Number)
- `next_key_id` (String)
- `next_signing_secret` (String, Sensitive)
- `signing_revision` (Number)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Replace illustrative identifiers with the existing object identity.
terraform import semaphore_ex_audit_webhook.audit audit_webhook
```
