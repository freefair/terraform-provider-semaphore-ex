---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_ldap_group_apply Action - Semaphore EX"
subcategory: ""
description: |-
  Applies a fresh LDAP mapping preview explicitly. This changes role assignments; stale previews fail without retry. Executes only through explicit Action invocation. Progress reports a bounded, non-secret summary.
  Known limitation: HashiCorp Terraform Plugin Framework v1.19.0 rejects non-null write-only Action inputs during plan validation (also reproduced with Terraform 1.16.3). This Action fails before invocation when such inputs are set; required write-only inputs make the Action unavailable. Ordinary resource write-only attributes and ephemeral preview resources are unaffected. The provider retains write-only protection and uses the unmodified official framework.
---

# semaphore_ex_ldap_group_apply (Action)

Applies a fresh LDAP mapping preview explicitly. This changes role assignments; stale previews fail without retry. Executes only through explicit Action invocation. Progress reports a bounded, non-secret summary.

Known limitation: HashiCorp Terraform Plugin Framework v1.19.0 rejects non-null write-only Action inputs during plan validation (also reproduced with Terraform 1.16.3). This Action fails before invocation when such inputs are set; required write-only inputs make the Action unavailable. Ordinary resource write-only attributes and ephemeral preview resources are unaffected. The provider retains write-only protection and uses the unmodified official framework.

## Example Usage

```terraform
variable "preview_token" {
  type      = string
  sensitive = true
  ephemeral = true
}

action "semaphore_ex_ldap_group_apply" "example" {
  config {
    provider_id   = "directory"
    preview_token = var.preview_token
  }
}

# terraform apply -invoke=action.semaphore_ex_ldap_group_apply.example
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `preview_token` (String, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Fresh preview token from the same LDAP provider; the server rechecks mapping and directory revisions.
- `provider_id` (String) Configured identity provider ID.
