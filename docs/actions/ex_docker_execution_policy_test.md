---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphore_ex_docker_execution_policy_test Action - Semaphore EX"
subcategory: ""
description: |-
  Evaluates a Docker execution request against stored policy. No container is created. Executes only through explicit Action invocation. Progress reports a bounded, non-secret summary. Full preview results are available through the matching ephemeral resource.
---

# semaphore_ex_docker_execution_policy_test (Action)

Evaluates a Docker execution request against stored policy. No container is created. Executes only through explicit Action invocation. Progress reports a bounded, non-secret summary. Full preview results are available through the matching ephemeral resource.

## Example Usage

```terraform
action "semaphore_ex_docker_execution_policy_test" "example" {
  config {
    request = { image = "example/image", privileged = false }
  }
}

# terraform apply -invoke=action.semaphore_ex_docker_execution_policy_test.example
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `request` (Attributes) (see [below for nested schema](#nestedatt--request))

<a id="nestedatt--request"></a>
### Nested Schema for `request`

Optional:

- `has_bind_mounts` (Boolean)
- `has_capabilities` (Boolean)
- `has_devices` (Boolean)
- `has_host_namespaces` (Boolean)
- `image` (String)
- `memory_bytes` (Number)
- `nano_cpus` (Number)
- `network` (String)
- `pids_limit` (Number)
- `privileged` (Boolean)
- `read_only_rootfs` (Boolean)
- `user` (String)
