ephemeral "semaphore_ex_global_policy_guardrail_validate" "example" {
  source_yaml = yamlencode({ version = 1, rules = [] })
}

# Reference ephemeral.semaphore_ex_global_policy_guardrail_validate.example.result in an ephemeral context.
